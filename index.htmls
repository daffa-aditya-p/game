<!DOCTYPE html>
<html lang="id" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIJA-1 - Elite Digital Academy</title>
    
    <!-- Meta Tags -->
    <meta name="description" content="Kelas 10 SIJA-1 SMKN 26 Jakarta - Elite Digital Academy 2025/2026">
    <meta name="keywords" content="SIJA, SMKN 26 Jakarta, Web3, Blockchain, Digital Academy">
    
    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🚀</text></svg>">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        'cyber': ['Orbitron', 'monospace'],
                        'modern': ['Space Grotesk', 'sans-serif']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Swiper CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
    
    <!-- AOS Animation -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <!-- Three.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    
    <!-- GSAP -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    
    <style>
        :root {
            /* Cyber Color Palette */
            --cyber-primary: #00ff88;
            --cyber-secondary: #ff0080;
            --cyber-accent: #00d4ff;
            --cyber-warning: #ffb800;
            --cyber-purple: #a855f7;
            --cyber-blue: #3b82f6;
            
            /* Gradients */
            --gradient-cyber: linear-gradient(135deg, #00ff88 0%, #00d4ff 50%, #ff0080 100%);
            --gradient-matrix: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
            --gradient-neon: linear-gradient(135deg, #ff0080 0%, #00d4ff 50%, #00ff88 100%);
            --gradient-dark: linear-gradient(135deg, #000000 0%, #1a1a2e 50%, #16213e 100%);
            
            /* Glass Effects */
            --glass-bg: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(255, 255, 255, 0.1);
            --glass-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
            
            /* Neon Shadows */
            --neon-green: 0 0 20px #00ff88, 0 0 40px #00ff88, 0 0 60px #00ff88;
            --neon-pink: 0 0 20px #ff0080, 0 0 40px #ff0080, 0 0 60px #ff0080;
            --neon-blue: 0 0 20px #00d4ff, 0 0 40px #00d4ff, 0 0 60px #00d4ff;
            
            /* Theme Colors */
            --bg-primary: #000000;
            --bg-secondary: #0a0a0a;
            --text-primary: #ffffff;
            --text-secondary: #a0a0a0;
            --border-color: rgba(255, 255, 255, 0.1);
        }
        
        [data-theme="light"] {
            --bg-primary: #f8fafc;
            --bg-secondary: #ffffff;
            --text-primary: #1e293b;
            --text-secondary: #64748b;
            --border-color: rgba(0, 0, 0, 0.1);
            --glass-bg: rgba(255, 255, 255, 0.7);
            --glass-border: rgba(255, 255, 255, 0.9);
        }
        
        * {
            font-family: 'Space Grotesk', sans-serif;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        body {
            background: var(--bg-primary);
            color: var(--text-primary);
            overflow-x: hidden;
            font-family: 'Space Grotesk', sans-serif;
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: var(--bg-secondary);
        }
        
        ::-webkit-scrollbar-thumb {
            background: var(--gradient-cyber);
            border-radius: 10px;
        }
        
        /* Matrix Rain Background */
        #matrix-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            opacity: 0.3;
        }
        
        /* Loading Screen */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-dark);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            transition: opacity 0.8s ease, visibility 0.8s ease;
        }
        
        .loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
        }
        
        .loading-spinner {
            width: 80px;
            height: 80px;
            border: 4px solid transparent;
            border-top: 4px solid var(--cyber-primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            position: relative;
        }
        
        .loading-spinner::before {
            content: '';
            position: absolute;
            top: -4px;
            left: -4px;
            right: -4px;
            bottom: -4px;
            border: 4px solid transparent;
            border-bottom: 4px solid var(--cyber-secondary);
            border-radius: 50%;
            animation: spin 2s linear infinite reverse;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* Cyber Typography */
        .cyber-title {
            font-family: 'Orbitron', monospace;
            font-weight: 900;
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-transform: uppercase;
            letter-spacing: 3px;
        }
        
        .cyber-subtitle {
            font-family: 'Orbitron', monospace;
            font-weight: 700;
            color: var(--cyber-accent);
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .glitch-text {
            position: relative;
            animation: glitch 2s infinite;
        }
        
        .glitch-text::before,
        .glitch-text::after {
            content: attr(data-text);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }
        
        .glitch-text::before {
            color: var(--cyber-secondary);
            animation: glitch-1 0.5s infinite;
            clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
        }
        
        .glitch-text::after {
            color: var(--cyber-accent);
            animation: glitch-2 0.5s infinite;
            clip-path: polygon(0 80%, 100% 20%, 100% 100%, 0 100%);
        }
        
        @keyframes glitch {
            0%, 74%, 100% { transform: translate(0); }
            75% { transform: translate(-2px, 2px); }
            76% { transform: translate(2px, -2px); }
            77% { transform: translate(-2px, -2px); }
            78% { transform: translate(2px, 2px); }
            79% { transform: translate(-2px, 2px); }
            80% { transform: translate(2px, -2px); }
        }
        
        @keyframes glitch-1 {
            0%, 100% { clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%); }
            25% { clip-path: polygon(0 60%, 100% 60%, 100% 100%, 0 100%); }
            50% { clip-path: polygon(0 20%, 100% 20%, 100% 80%, 0 80%); }
            75% { clip-path: polygon(0 40%, 100% 40%, 100% 60%, 0 60%); }
        }
        
        @keyframes glitch-2 {
            0%, 100% { clip-path: polygon(0 80%, 100% 20%, 100% 100%, 0 100%); }
            25% { clip-path: polygon(0 0%, 100% 0%, 100% 40%, 0 40%); }
            50% { clip-path: polygon(0 60%, 100% 60%, 100% 100%, 0 100%); }
            75% { clip-path: polygon(0 20%, 100% 20%, 100% 80%, 0 80%); }
        }
        
        /* Navbar */
        .cyber-navbar {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--glass-border);
            box-shadow: var(--glass-shadow);
        }
        
        .nav-link-cyber {
            position: relative;
            color: var(--text-primary);
            text-decoration: none;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 12px 20px;
            border-radius: 25px;
            transition: all 0.3s ease;
            overflow: hidden;
        }
        
        .nav-link-cyber::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient-cyber);
            transition: left 0.3s ease;
            z-index: -1;
        }
        
        .nav-link-cyber:hover::before {
            left: 0;
        }
        
        .nav-link-cyber:hover {
            color: #000000;
            transform: translateY(-2px);
            box-shadow: var(--neon-green);
        }
        
        /* Theme Toggle */
        .theme-toggle {
            position: relative;
            width: 70px;
            height: 35px;
            background: var(--gradient-matrix);
            border-radius: 50px;
            border: 2px solid var(--cyber-primary);
            cursor: pointer;
            transition: all 0.3s ease;
            overflow: hidden;
        }
        
        .theme-toggle::before {
            content: '';
            position: absolute;
            top: 3px;
            left: 3px;
            width: 25px;
            height: 25px;
            background: var(--gradient-cyber);
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--neon-green);
        }
        
        [data-theme="light"] .theme-toggle::before {
            transform: translateX(32px);
            box-shadow: var(--neon-blue);
        }
        
        .theme-toggle:hover {
            box-shadow: var(--neon-green);
            transform: scale(1.1);
        }
        
        /* Hero Section */
        .hero-cyber {
            min-height: 100vh;
            background: var(--gradient-dark);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
        }
        
        .hero-cyber::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000"><defs><radialGradient id="grad1" cx="50%" cy="50%"><stop offset="0%" style="stop-color:rgba(0,255,136,0.1)"/><stop offset="100%" style="stop-color:rgba(0,255,136,0)"/></radialGradient><radialGradient id="grad2" cx="50%" cy="50%"><stop offset="0%" style="stop-color:rgba(255,0,128,0.1)"/><stop offset="100%" style="stop-color:rgba(255,0,128,0)"/></radialGradient></defs><circle cx="200" cy="300" r="150" fill="url(%23grad1)"/><circle cx="800" cy="700" r="200" fill="url(%23grad2)"/><circle cx="500" cy="150" r="100" fill="url(%23grad1)"/></svg>') no-repeat center center/cover;
            animation: float-bg 20s ease-in-out infinite;
        }
        
        @keyframes float-bg {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-30px) rotate(5deg); }
        }
        
        .floating-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        
        .particle {
            position: absolute;
            background: var(--cyber-primary);
            border-radius: 50%;
            animation: float-particle 8s infinite linear;
        }
        
        @keyframes float-particle {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100px) rotate(360deg);
                opacity: 0;
            }
        }
        
        /* Glass Cards */
        .glass-card-cyber {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            box-shadow: var(--glass-shadow);
            transition: all 0.4s cubic-bezier(0.23, 1, 0.320, 1);
            position: relative;
            overflow: hidden;
        }
        
        .glass-card-cyber::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.6s ease;
        }
        
        .glass-card-cyber:hover::before {
            left: 100%;
        }
        
        .glass-card-cyber:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: var(--neon-green), var(--glass-shadow);
            border-color: var(--cyber-primary);
        }
        
        /* Hologram Effect */
        .hologram-card {
            position: relative;
            background: linear-gradient(45deg, rgba(0,255,136,0.1), rgba(255,0,128,0.1));
            border: 2px solid transparent;
            border-image: var(--gradient-cyber) 1;
            border-radius: 20px;
            overflow: hidden;
        }
        
        .hologram-card::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-cyber);
            border-radius: 20px;
            z-index: -1;
            animation: hologram-border 3s ease-in-out infinite alternate;
        }
        
        @keyframes hologram-border {
            0% { opacity: 0.5; transform: scale(1); }
            100% { opacity: 1; transform: scale(1.02); }
        }
        
        /* Cyber Buttons */
        .btn-cyber {
            background: var(--gradient-cyber);
            border: none;
            border-radius: 50px;
            color: #000000;
            font-weight: 700;
            font-family: 'Orbitron', monospace;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 15px 40px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .btn-cyber::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.5s ease;
        }
        
        .btn-cyber:hover::before {
            left: 100%;
        }
        
        .btn-cyber:hover {
            transform: translateY(-3px);
            box-shadow: var(--neon-green);
            color: #000000;
        }
        
        .btn-cyber-outline {
            background: transparent;
            border: 2px solid var(--cyber-primary);
            color: var(--cyber-primary);
        }
        
        .btn-cyber-outline:hover {
            background: var(--cyber-primary);
            color: #000000;
            box-shadow: var(--neon-green);
        }
        
        /* Schedule Cards */
        .schedule-card-cyber {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            transition: all 0.4s cubic-bezier(0.23, 1, 0.320, 1);
            position: relative;
            overflow: hidden;
        }
        
        .schedule-card-cyber::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-cyber);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }
        
        .schedule-card-cyber:hover::after {
            transform: scaleX(1);
        }
        
        .schedule-card-cyber:hover {
            transform: translateY(-10px) rotateX(5deg);
            box-shadow: var(--neon-blue);
            border-color: var(--cyber-accent);
        }
        
        .subject-cyber {
            background: linear-gradient(135deg, rgba(0,255,136,0.1), rgba(0,212,255,0.1));
            border-left: 4px solid var(--cyber-primary);
            border-radius: 12px;
            padding: 12px 16px;
            margin: 8px 0;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .subject-cyber::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 0;
            background: var(--gradient-cyber);
            transition: width 0.3s ease;
            border-radius: 12px;
        }
        
        .subject-cyber:hover::before {
            width: 100%;
            opacity: 0.1;
        }
        
        .subject-cyber:hover {
            transform: translateX(10px);
            border-left-color: var(--cyber-accent);
            box-shadow: 0 5px 15px rgba(0,255,136,0.3);
        }
        
        /* Swiper Customization */
        .swiper-pagination-bullet {
            background: var(--cyber-primary);
            opacity: 0.5;
        }
        
        .swiper-pagination-bullet-active {
            background: var(--cyber-primary);
            opacity: 1;
            box-shadow: var(--neon-green);
        }
        
        .swiper-button-next,
        .swiper-button-prev {
            color: var(--cyber-primary);
            background: var(--glass-bg);
            border-radius: 50%;
            width: 50px;
            height: 50px;
            margin-top: -25px;
        }
        
        .swiper-button-next:after,
        .swiper-button-prev:after {
            font-size: 16px;
        }
        
        /* Gallery */
        .gallery-item {
            border-radius: 20px;
            overflow: hidden;
            position: relative;
            transition: all 0.4s ease;
        }
        
        .gallery-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-cyber);
            opacity: 0;
            transition: opacity 0.3s ease;
            z-index: 1;
            mix-blend-mode: overlay;
        }
        
        .gallery-item:hover::before {
            opacity: 0.3;
        }
        
        .gallery-item:hover {
            transform: scale(1.05);
            box-shadow: var(--neon-green);
        }
        
        /* Footer */
        .footer-cyber {
            background: var(--gradient-dark);
            position: relative;
            overflow: hidden;
        }
        
        .footer-cyber::before {
            content: '';
            position: absolute;
            top: -100px;
            left: 0;
            right: 0;
            height: 100px;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none"><path d="M985.66,92.83C906.67,72,823.78,31,743.84,14.19c-82.26-17.34-168.06-16.33-250.45.39-57.84,11.73-114,31.07-172,41.86A600.21,600.21,0,0,1,0,27.35V120H1200V95.8C1132.19,118.92,1055.71,111.31,985.66,92.83Z" fill="%23000000"></path></svg>') no-repeat center center/cover;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .cyber-title {
                font-size: 3rem;
                line-height: 1.2;
            }
            
            .glass-card-cyber:hover {
                transform: translateY(-10px) scale(1);
            }
            
            .floating-particles {
                display: none;
            }
        }
        
        /* Custom Animations */
        .fade-in-up {
            animation: fadeInUp 0.8s ease forwards;
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .slide-in-left {
            animation: slideInLeft 0.8s ease forwards;
        }
        
        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        .slide-in-right {
            animation: slideInRight 0.8s ease forwards;
        }
        
        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        /* Scan Line Effect */
        .scan-line {
            position: relative;
            overflow: hidden;
        }
        
        .scan-line::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0,255,136,0.3), transparent);
            animation: scan 3s ease-in-out infinite;
        }
        
        @keyframes scan {
            0% { left: -100%; }
            100% { left: 100%; }
        }
        
        /* Pulsing Elements */
        .pulse-cyber {
            animation: pulseCyber 2s ease-in-out infinite alternate;
        }
        
        @keyframes pulseCyber {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            100% {
                transform: scale(1.05);
                opacity: 0.8;
            }
        }
    </style>
</head>
<body>
    <!-- Matrix Background -->
    <canvas id="matrix-bg"></canvas>
    
    <!-- Loading Screen -->
    <div class="loading-screen" id="loadingScreen">
        <div class="loading-spinner"></div>
        <h3 class="cyber-subtitle mt-4">INITIALIZING SYSTEM...</h3>
        <div class="progress mt-3" style="width: 200px; height: 4px;">
            <div class="progress-bar" style="background: var(--gradient-cyber); width: 0%; animation: loading 3s ease-in-out forwards;"></div>
        </div>
    </div>
    
    <!-- Floating Particles -->
    <div class="floating-particles" id="particles"></div>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg fixed-top cyber-navbar">
        <div class="container">
            <a class="navbar-brand cyber-title fs-2" href="#" data-aos="fade-right">
                <i class="fas fa-cube me-2"></i>SIJA-1
            </a>
            
            <div class="d-flex align-items-center order-lg-3">
                <div class="theme-toggle me-3" onclick="toggleTheme()" title="Toggle Theme"></div>
                <button class="navbar-toggler border-0 p-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <div class="hamburger">
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                </button>
            </div>
            
            <div class="collapse navbar-collapse order-lg-2" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item mx-1">
                        <a class="nav-link-cyber" href="#home">
                            <i class="fas fa-home me-2"></i>Home
                        </a>
                    </li>
                    <li class="nav-item mx-1">
                        <a class="nav-link-cyber" href="#jadwal">
                            <i class="fas fa-calendar-alt me-2"></i>Schedule
                        </a>
                    </li>
                    <li class="nav-item mx-1">
                        <a class="nav-link-cyber" href="#galeri">
                            <i class="fas fa-images me-2"></i>Gallery
                        </a>
                    </li>
                    <li class="nav-item mx-1">
                        <a class="nav-link-cyber" href="#kontak">
                            <i class="fas fa-envelope me-2"></i>Contact
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="home" class="hero-cyber">
        <div class="container position-relative z-3">
            <div class="row align-items-center min-vh-100">
                <div class="col-lg-6" data-aos="fade-up" data-aos-delay="500">
                    <div class="mb-4">
                        <span class="cyber-subtitle fs-5 pulse-cyber">// ELITE DIGITAL ACADEMY</span>
                    </div>
                    <h1 class="cyber-title display-1 mb-4 glitch-text" data-text="SIJA-1">
                        SIJA-1
                    </h1>
                    <h2 class="cyber-subtitle fs-2 mb-4">
                        WEB3 × BLOCKCHAIN × AI
                    </h2>
                    <p class="fs-4 mb-4" style="color: var(--text-secondary);">
                        SMKN 26 JAKARTA • 2025/2026
                    </p>
                    <p class="fs-5 mb-5" style="color: var(--text-secondary);">
                        Sistem Informasi, Jaringan & Aplikasi
                    </p>
                    <div class="d-flex flex-wrap gap-3">
                        <button class="btn-cyber" onclick="scrollToSection('galeri')">
                            <i class="fas fa-rocket me-2"></i>EXPLORE NOW
                        </button>
                        <button class="btn-cyber btn-cyber-outline" onclick="scrollToSection('jadwal')">
                            <i class="fas fa-code me-2"></i>VIEW SCHEDULE
                        </button>
                    </div>
                </div>
                <div class="col-lg-6" data-aos="fade-left" data-aos-delay="800">
                    <div class="position-relative">
                        <div id="threejs-container" style="width: 100%; height: 500px; border-radius: 20px; overflow: hidden;"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Gallery Section -->
    <section id="galeri" class="py-5 position-relative" style="background: var(--bg-secondary);">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="cyber-title display-4 mb-4" data-aos="fade-up">
                    DIGITAL GALLERY
                </h2>
                <p class="cyber-subtitle fs-5" data-aos="fade-up" data-aos-delay="200">
                    // CAPTURED MOMENTS IN THE MATRIX
                </p>
            </div>
            
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <div class="swiper gallery-swiper glass-card-cyber p-4 scan-line" data-aos="zoom-in" data-aos-delay="400">
                        <div class="swiper-wrapper">
                            <div class="swiper-slide">
                                <div class="gallery-item">
                                    <img src="https://picsum.photos/900/600?random=1&blur=0" class="w-100 h-auto" alt="Cyber Learning">
                                    <div class="position-absolute bottom-0 start-0 end-0 p-4 bg-gradient-to-t from-black via-transparent to-transparent">
                                        <h5 class="text-white fw-bold cyber-subtitle">CYBER LEARNING PROTOCOL</h5>
                                        <p class="text-white-50 mb-0">Advanced programming sessions</p>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="gallery-item">
                                    <img src="https://picsum.photos/900/600?random=2&blur=0" class="w-100 h-auto" alt="AI Workshop">
                                    <div class="position-absolute bottom-0 start-0 end-0 p-4 bg-gradient-to-t from-black via-transparent to-transparent">
                                        <h5 class="text-white fw-bold cyber-subtitle">AI WORKSHOP 2.0</h5>
                                        <p class="text-white-50 mb-0">Machine learning implementation</p>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="gallery-item">
                                    <img src="https://picsum.photos/900/600?random=3&blur=0" class="w-100 h-auto" alt="Blockchain Lab">
                                    <div class="position-absolute bottom-0 start-0 end-0 p-4 bg-gradient-to-t from-black via-transparent to-transparent">
                                        <h5 class="text-white fw-bold cyber-subtitle">BLOCKCHAIN LAB</h5>
                                        <p class="text-white-50 mb-0">Cryptocurrency development</p>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="gallery-item">
                                    <img src="https://picsum.photos/900/600?random=4&blur=0" class="w-100 h-auto" alt="Web3 Project">
                                    <div class="position-absolute bottom-0 start-0 end-0 p-4 bg-gradient-to-t from-black via-transparent to-transparent">
                                        <h5 class="text-white fw-bold cyber-subtitle">WEB3 PROJECT</h5>
                                        <p class="text-white-50 mb-0">Decentralized applications</p>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="gallery-item">
                                    <img src="https://picsum.photos/900/600?random=5&blur=0" class="w-100 h-auto" alt="Metaverse Event">
                                    <div class="position-absolute bottom-0 start-0 end-0 p-4 bg-gradient-to-t from-black via-transparent to-transparent">
                                        <h5 class="text-white fw-bold cyber-subtitle">METAVERSE EVENT</h5>
                                        <p class="text-white-50 mb-0">Virtual reality showcase</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="swiper-pagination"></div>
                        <div class="swiper-button-next"></div>
                        <div class="swiper-button-prev"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Schedule Duty Section -->
    <section class="py-5" style="background: var(--bg-primary);">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="cyber-title display-4 mb-4" data-aos="fade-up">
                    DUTY SCHEDULER
                </h2>
                <p class="cyber-subtitle fs-5" data-aos="fade-up" data-aos-delay="200">
                    // AUTOMATED TASK ASSIGNMENT MATRIX
                </p>
            </div>
            
            <div class="row g-4">
                <div class="col-md-6 col-xl-4" data-aos="fade-up" data-aos-delay="100">
                    <div class="hologram-card glass-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-cyber); box-shadow: var(--neon-green);">
                                <i class="fas fa-calendar-week text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">MONDAY.EXE</h5>
                        </div>
                        <div class="space-y-3">
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-primary);">
                                <span style="color: var(--cyber-primary);">→ AGENT_001</span>
                            </div>
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-primary);">
                                <span style="color: var(--cyber-primary);">→ AGENT_002</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-6 col-xl-4" data-aos="fade-up" data-aos-delay="200">
                    <div class="hologram-card glass-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-neon); box-shadow: var(--neon-pink);">
                                <i class="fas fa-calendar-week text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">TUESDAY.EXE</h5>
                        </div>
                        <div class="space-y-3">
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-secondary);">
                                <span style="color: var(--cyber-secondary);">→ AGENT_003</span>
                            </div>
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-secondary);">
                                <span style="color: var(--cyber-secondary);">→ AGENT_004</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-6 col-xl-4" data-aos="fade-up" data-aos-delay="300">
                    <div class="hologram-card glass-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: linear-gradient(135deg, var(--cyber-accent), var(--cyber-blue)); box-shadow: var(--neon-blue);">
                                <i class="fas fa-calendar-week text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">WEDNESDAY.EXE</h5>
                        </div>
                        <div class="space-y-3">
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-accent);">
                                <span style="color: var(--cyber-accent);">→ AGENT_005</span>
                            </div>
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-accent);">
                                <span style="color: var(--cyber-accent);">→ AGENT_006</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-6 col-xl-4" data-aos="fade-up" data-aos-delay="400">
                    <div class="hologram-card glass-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-cyber); box-shadow: var(--neon-green);">
                                <i class="fas fa-calendar-week text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">THURSDAY.EXE</h5>
                        </div>
                        <div class="space-y-3">
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-primary);">
                                <span style="color: var(--cyber-primary);">→ AGENT_007</span>
                            </div>
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-primary);">
                                <span style="color: var(--cyber-primary);">→ AGENT_008</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="col-md-6 col-xl-4" data-aos="fade-up" data-aos-delay="500">
                    <div class="hologram-card glass-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-neon); box-shadow: var(--neon-pink);">
                                <i class="fas fa-calendar-week text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">FRIDAY.EXE</h5>
                        </div>
                        <div class="space-y-3">
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-secondary);">
                                <span style="color: var(--cyber-secondary);">→ AGENT_009</span>
                            </div>
                            <div class="p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-secondary);">
                                <span style="color: var(--cyber-secondary);">→ AGENT_010</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Subject Schedule Section -->
    <section id="jadwal" class="py-5" style="background: var(--bg-secondary);">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="cyber-title display-4 mb-4" data-aos="fade-up">
                    LEARNING PROTOCOL
                </h2>
                <p class="cyber-subtitle fs-5" data-aos="fade-up" data-aos-delay="200">
                    // ADVANCED CURRICULUM MATRIX
                </p>
            </div>
            
            <div class="row g-4">
                <div class="col-lg-6 col-xl-4" data-aos="fade-up" data-aos-delay="100">
                    <div class="schedule-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-cyber); box-shadow: var(--neon-green);">
                                <i class="fas fa-code text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">MONDAY.SYS</h5>
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-laptop-code me-2"></i>Dasar Pengembangan Perangkat Lunak
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-microchip me-2"></i>Informatika
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-6 col-xl-4" data-aos="fade-up" data-aos-delay="200">
                    <div class="schedule-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-neon); box-shadow: var(--neon-pink);">
                                <i class="fas fa-brain text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">TUESDAY.SYS</h5>
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-calculator me-2"></i>Matematika
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-flask me-2"></i>Projek IPAS
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-language me-2"></i>Bahasa Jepang
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-palette me-2"></i>Seni Budaya
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-6 col-xl-4" data-aos="fade-up" data-aos-delay="300">
                    <div class="schedule-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: linear-gradient(135deg, var(--cyber-accent), var(--cyber-blue)); box-shadow: var(--neon-blue);">
                                <i class="fas fa-database text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">WEDNESDAY.SYS</h5>
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-laptop-code me-2"></i>Dasar Pengembangan Perangkat Lunak
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-book me-2"></i>Bahasa Indonesia
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-6 col-xl-4" data-aos="fade-up" data-aos-delay="400">
                    <div class="schedule-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-cyber); box-shadow: var(--neon-green);">
                                <i class="fas fa-atom text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">THURSDAY.SYS</h5>
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-flask me-2"></i>Projek IPAS
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-running me-2"></i>Penjasorkes
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-mosque me-2"></i>Pendidikan Agama Islam dan Budi Pekerti
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-6 col-xl-4" data-aos="fade-up" data-aos-delay="500">
                    <div class="schedule-card-cyber p-4 h-100">
                        <div class="d-flex align-items-center mb-4">
                            <div class="p-3 rounded-circle me-3" style="background: var(--gradient-neon); box-shadow: var(--neon-pink);">
                                <i class="fas fa-globe text-dark fs-5"></i>
                            </div>
                            <h5 class="cyber-subtitle mb-0">FRIDAY.SYS</h5>
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-comments me-2"></i>Bahasa Inggris
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-balance-scale me-2"></i>PPKn
                        </div>
                        <div class="subject-cyber">
                            <i class="fas fa-landmark me-2"></i>Sejarah
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="kontak" class="footer-cyber text-white py-5 position-relative">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 mb-4" data-aos="fade-up" data-aos-delay="100">
                    <h4 class="cyber-title fs-3 mb-4">SIJA-1</h4>
                    <p class="mb-3" style="color: var(--text-secondary);">
                        Elite Digital Academy - Advanced Technology Institute
                    </p>
                    <p class="mb-4" style="color: var(--text-secondary);">
                        SMKN 26 Jakarta • Cyber Division 2025/2026
                    </p>
                    <div class="d-flex gap-3">
                        <a href="#" class="p-3 rounded-circle text-decoration-none" style="background: var(--gradient-cyber); color: #000000; transition: all 0.3s ease;" onmouseover="this.style.boxShadow='var(--neon-green)'" onmouseout="this.style.boxShadow='none'">
                            <i class="fab fa-instagram fs-5"></i>
                        </a>
                        <a href="#" class="p-3 rounded-circle text-decoration-none" style="background: var(--gradient-neon); color: #000000; transition: all 0.3s ease;" onmouseover="this.style.boxShadow='var(--neon-pink)'" onmouseout="this.style.boxShadow='none'">
                            <i class="fab fa-youtube fs-5"></i>
                        </a>
                        <a href="#" class="p-3 rounded-circle text-decoration-none" style="background: linear-gradient(135deg, var(--cyber-accent), var(--cyber-blue)); color: #000000; transition: all 0.3s ease;" onmouseover="this.style.boxShadow='var(--neon-blue)'" onmouseout="this.style.boxShadow='none'">
                            <i class="fab fa-whatsapp fs-5"></i>
                        </a>
                        <a href="#" class="p-3 rounded-circle text-decoration-none" style="background: var(--gradient-cyber); color: #000000; transition: all 0.3s ease;" onmouseover="this.style.boxShadow='var(--neon-green)'" onmouseout="this.style.boxShadow='none'">
                            <i class="fab fa-discord fs-5"></i>
                        </a>
                    </div>
                </div>
                
                <div class="col-lg-4 mb-4" data-aos="fade-up" data-aos-delay="200">
                    <h5 class="cyber-subtitle mb-4">CONTACT PROTOCOL</h5>
                    <div class="d-flex align-items-center mb-3">
                        <i class="fas fa-map-marker-alt me-3 fs-5" style="color: var(--cyber-primary);"></i>
                        <span style="color: var(--text-secondary);">SMKN 26 Jakarta, Cyber District</span>
                    </div>
                    <div class="d-flex align-items-center mb-3">
                        <i class="fas fa-envelope me-3 fs-5" style="color: var(--cyber-secondary);"></i>
                        <span style="color: var(--text-secondary);">sija1@cyberacademy.id</span>
                    </div>
                    <div class="d-flex align-items-center mb-3">
                        <i class="fas fa-phone me-3 fs-5" style="color: var(--cyber-accent);"></i>
                        <span style="color: var(--text-secondary);">+62 21 2025 2026</span>
                    </div>
                    <div class="d-flex align-items-center">
                        <i class="fas fa-globe me-3 fs-5" style="color: var(--cyber-primary);"></i>
                        <span style="color: var(--text-secondary);">www.sija1.web3.id</span>
                    </div>
                </div>
                
                <div class="col-lg-4 mb-4" data-aos="fade-up" data-aos-delay="300">
                    <h5 class="cyber-subtitle mb-4">QUICK ACCESS</h5>
                    <div class="row">
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <a href="#home" class="text-decoration-none" style="color: var(--text-secondary); transition: color 0.3s ease;" onmouseover="this.style.color='var(--cyber-primary)'" onmouseout="this.style.color='var(--text-secondary)'">
                                        <i class="fas fa-chevron-right me-2"></i>Home
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#galeri" class="text-decoration-none" style="color: var(--text-secondary); transition: color 0.3s ease;" onmouseover="this.style.color='var(--cyber-primary)'" onmouseout="this.style.color='var(--text-secondary)'">
                                        <i class="fas fa-chevron-right me-2"></i>Gallery
                                    </a>
                                </li>
                            </ul>
                        </div>
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <a href="#jadwal" class="text-decoration-none" style="color: var(--text-secondary); transition: color 0.3s ease;" onmouseover="this.style.color='var(--cyber-primary)'" onmouseout="this.style.color='var(--text-secondary)'">
                                        <i class="fas fa-chevron-right me-2"></i>Schedule
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#kontak" class="text-decoration-none" style="color: var(--text-secondary); transition: color 0.3s ease;" onmouseover="this.style.color='var(--cyber-primary)'" onmouseout="this.style.color='var(--text-secondary)'">
                                        <i class="fas fa-chevron-right me-2"></i>Contact
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                    
                    <div class="mt-4 p-3 rounded-3" style="background: var(--glass-bg); border: 1px solid var(--cyber-primary);">
                        <h6 class="cyber-subtitle fs-6 mb-2">SYSTEM STATUS</h6>
                        <div class="d-flex align-items-center">
                            <div class="pulse-cyber me-2" style="width: 12px; height: 12px; background: var(--cyber-primary); border-radius: 50%;"></div>
                            <span style="color: var(--cyber-primary); font-family: 'Orbitron', monospace;">ONLINE • ALL SYSTEMS OPERATIONAL</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <hr class="my-4" style="border-color: var(--border-color);">
            
            <div class="text-center">
                <p class="mb-2" style="color: var(--text-secondary);">
                    &copy; 2025 SIJA-1 Elite Digital Academy. Powered by Web3 Technology.
                </p>
                <p class="mb-0 cyber-subtitle fs-6">
                    CRAFTED BY <span style="color: var(--cyber-primary);">daffa-aditya-p</span> • FUTURISTIC DESIGN
                </p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/js/all.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    
    <script>
        // Enhanced Theme Toggle
        function toggleTheme() {
            const html = document.documentElement;
            const currentTheme = html.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            
            // Animate theme transition
            document.body.style.transition = 'all 0.5s ease';
            setTimeout(() => {
                document.body.style.transition = '';
            }, 500);
        }

        // Load saved theme
        const savedTheme = localStorage.getItem('theme') || 'dark';
        document.documentElement.setAttribute('data-theme', savedTheme);

        // Enhanced Loading Screen with Progress
        let loadingProgress = 0;
        const loadingInterval = setInterval(() => {
            loadingProgress += Math.random() * 30;
            const progressBar = document.querySelector('.progress-bar');
            if (progressBar) {
                progressBar.style.width = Math.min(loadingProgress, 100) + '%';
            }
            
            if (loadingProgress >= 100) {
                clearInterval(loadingInterval);
                setTimeout(() => {
                    document.getElementById('loadingScreen').classList.add('hidden');
                    initializeAnimations();
                }, 500);
            }
        }, 100);

        // Matrix Rain Effect (continued)
        function initMatrixRain() {
            const canvas = document.getElementById('matrix-bg');
            const ctx = canvas.getContext('2d');
            
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            
            const matrix = "SIJA1SMKN26JAKARTA2025WEB3BLOCKCHAIN0123456789@#$%^&*()";
            const matrixArray = matrix.split("");
            
            const fontSize = 14;
            const columns = canvas.width / fontSize;
            const drops = [];
            
            for (let x = 0; x < columns; x++) {
                drops[x] = 1;
            }
            
            function drawMatrix() {
                ctx.fillStyle = 'rgba(0, 0, 0, 0.04)';
                ctx.fillRect(0, 0, canvas.width, canvas.height);
                
                ctx.fillStyle = '#00ff88';
                ctx.font = fontSize + 'px "Orbitron"';
                
                for (let i = 0; i < drops.length; i++) {
                    const text = matrixArray[Math.floor(Math.random() * matrixArray.length)];
                    ctx.fillText(text, i * fontSize, drops[i] * fontSize);
                    
                    if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) {
                        drops[i] = 0;
                    }
                    drops[i]++;
                }
            }
            
            setInterval(drawMatrix, 50);
            
            window.addEventListener('resize', () => {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            });
        }

        // Three.js 3D Scene
        function initThreeJS() {
            const container = document.getElementById('threejs-container');
            if (!container) return;
            
            const scene = new THREE.Scene();
            const camera = new THREE.PerspectiveCamera(75, container.offsetWidth / container.offsetHeight, 0.1, 1000);
            const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
            
            renderer.setSize(container.offsetWidth, container.offsetHeight);
            renderer.setClearColor(0x000000, 0.1);
            container.appendChild(renderer.domElement);
            
            // Create geometric shapes
            const geometry = new THREE.IcosahedronGeometry(1, 1);
            const material = new THREE.MeshBasicMaterial({
                color: 0x00ff88,
                wireframe: true,
                transparent: true,
                opacity: 0.8
            });
            
            const shapes = [];
            for (let i = 0; i < 20; i++) {
                const shape = new THREE.Mesh(geometry, material.clone());
                shape.position.set(
                    (Math.random() - 0.5) * 10,
                    (Math.random() - 0.5) * 10,
                    (Math.random() - 0.5) * 10
                );
                shape.rotation.set(
                    Math.random() * Math.PI,
                    Math.random() * Math.PI,
                    Math.random() * Math.PI
                );
                shapes.push(shape);
                scene.add(shape);
            }
            
            camera.position.z = 8;
            
            function animate() {
                requestAnimationFrame(animate);
                
                shapes.forEach((shape, index) => {
                    shape.rotation.x += 0.01 + index * 0.001;
                    shape.rotation.y += 0.01 + index * 0.001;
                    
                    // Floating effect
                    shape.position.y += Math.sin(Date.now() * 0.001 + index) * 0.002;
                    
                    // Color cycling
                    const hue = (Date.now() * 0.001 + index * 0.1) % 1;
                    shape.material.color.setHSL(hue, 1, 0.5);
                });
                
                renderer.render(scene, camera);
            }
            
            animate();
            
            // Responsive resize
            window.addEventListener('resize', () => {
                camera.aspect = container.offsetWidth / container.offsetHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(container.offsetWidth, container.offsetHeight);
            });
        }

        // Floating Particles
        function createFloatingParticles() {
            const container = document.getElementById('particles');
            const particleCount = window.innerWidth < 768 ? 30 : 60;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.width = Math.random() * 4 + 2 + 'px';
                particle.style.height = particle.style.width;
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = 8 + Math.random() * 4 + 's';
                
                // Random colors from cyber palette
                const colors = ['#00ff88', '#ff0080', '#00d4ff', '#ffb800'];
                particle.style.background = colors[Math.floor(Math.random() * colors.length)];
                particle.style.boxShadow = `0 0 10px ${particle.style.background}`;
                
                container.appendChild(particle);
                
                // Remove and recreate particle after animation
                setTimeout(() => {
                    if (particle.parentNode) {
                        particle.parentNode.removeChild(particle);
                    }
                }, (8 + Math.random() * 4) * 1000);
            }
        }

        // Enhanced Swiper Configuration
        function initSwiper() {
            const swiper = new Swiper('.gallery-swiper', {
                loop: true,
                centeredSlides: true,
                slidesPerView: 1,
                spaceBetween: 30,
                speed: 800,
                pagination: {
                    el: '.swiper-pagination',
                    clickable: true,
                    dynamicBullets: true
                },
                navigation: {
                    nextEl: '.swiper-button-next',
                    prevEl: '.swiper-button-prev',
                },
                autoplay: {
                    delay: 4000,
                    disableOnInteraction: false,
                    pauseOnMouseEnter: true
                },
                effect: 'coverflow',
                coverflowEffect: {
                    rotate: 30,
                    stretch: 0,
                    depth: 200,
                    modifier: 1,
                    slideShadows: true,
                },
                breakpoints: {
                    768: {
                        slidesPerView: 1.5,
                        spaceBetween: 40
                    },
                    1024: {
                        slidesPerView: 2,
                        spaceBetween: 50
                    }
                },
                on: {
                    slideChange: function() {
                        // Add sound effect here if needed
                        playSlideSound();
                    }
                }
            });
        }

        // Sound Effects (Optional)
        function playSlideSound() {
            // Web Audio API for cyber sound effects
            const audioContext = new (window.AudioContext || window.webkitAudioContext)();
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();
            
            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);
            
            oscillator.frequency.setValueAtTime(800, audioContext.currentTime);
            oscillator.frequency.exponentialRampToValueAtTime(400, audioContext.currentTime + 0.1);
            
            gainNode.gain.setValueAtTime(0, audioContext.currentTime);
            gainNode.gain.linearRampToValueAtTime(0.1, audioContext.currentTime + 0.01);
            gainNode.gain.exponentialRampToValueAtTime(0.001, audioContext.currentTime + 0.1);
            
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.1);
        }

        // Enhanced Scroll Animations with GSAP
        function initScrollAnimations() {
            gsap.registerPlugin(ScrollTrigger);
            
            // Hero text animations
            gsap.timeline()
                .from('.cyber-title', { 
                    duration: 1.5, 
                    y: 100, 
                    opacity: 0, 
                    ease: "power4.out" 
                })
                .from('.cyber-subtitle', { 
                    duration: 1, 
                    y: 50, 
                    opacity: 0, 
                    ease: "power3.out" 
                }, "-=1")
                .from('.btn-cyber', { 
                    duration: 0.8, 
                    scale: 0, 
                    opacity: 0, 
                    ease: "back.out(1.7)" 
                }, "-=0.5");
            
            // Cards animation on scroll
            gsap.utils.toArray('.glass-card-cyber').forEach((card, index) => {
                gsap.fromTo(card, 
                    { 
                        y: 100, 
                        opacity: 0, 
                        rotationX: -15 
                    },
                    {
                        y: 0,
                        opacity: 1,
                        rotationX: 0,
                        duration: 1,
                        ease: "power3.out",
                        scrollTrigger: {
                            trigger: card,
                            start: "top 80%",
                            end: "bottom 20%",
                            toggleActions: "play none none reverse"
                        },
                        delay: index * 0.1
                    }
                );
            });
            
            // Schedule cards with stagger effect
            gsap.utils.toArray('.schedule-card-cyber').forEach((card, index) => {
                gsap.fromTo(card,
                    {
                        x: index % 2 === 0 ? -100 : 100,
                        opacity: 0,
                        rotationY: index % 2 === 0 ? -15 : 15
                    },
                    {
                        x: 0,
                        opacity: 1,
                        rotationY: 0,
                        duration: 1.2,
                        ease: "power3.out",
                        scrollTrigger: {
                            trigger: card,
                            start: "top 85%",
                            toggleActions: "play none none reverse"
                        },
                        delay: index * 0.15
                    }
                );
            });
        }

        // Enhanced Navigation
        function initNavigation() {
            // Smooth scroll with easing
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    const targetElement = document.querySelector(targetId);
                    
                    if (targetElement) {
                        gsap.to(window, {
                            duration: 1.5,
                            scrollTo: {
                                y: targetElement,
                                offsetY: 80
                            },
                            ease: "power3.inOut"
                        });
                    }
                });
            });
            
            // Navbar background on scroll
            ScrollTrigger.create({
                start: "top -80",
                end: 99999,
                toggleClass: {
                    className: "scrolled",
                    targets: ".cyber-navbar"
                }
            });
        }

        // Scroll to section function
        function scrollToSection(sectionId) {
            const targetElement = document.getElementById(sectionId);
            if (targetElement) {
                gsap.to(window, {
                    duration: 1.5,
                    scrollTo: {
                        y: targetElement,
                        offsetY: 80
                    },
                    ease: "power3.inOut"
                });
            }
        }

        // Enhanced Cursor Effect
        function initCursor() {
            if (window.innerWidth > 768) {
                const cursor = document.createElement('div');
                cursor.className = 'custom-cursor';
                cursor.style.cssText = `
                    position: fixed;
                    width: 20px;
                    height: 20px;
                    background: var(--gradient-cyber);
                    border-radius: 50%;
                    pointer-events: none;
                    z-index: 9999;
                    transition: transform 0.1s ease;
                    mix-blend-mode: difference;
                `;
                document.body.appendChild(cursor);
                
                let mouseX = 0, mouseY = 0;
                let cursorX = 0, cursorY = 0;
                
                document.addEventListener('mousemove', (e) => {
                    mouseX = e.clientX;
                    mouseY = e.clientY;
                });
                
                function updateCursor() {
                    cursorX += (mouseX - cursorX) * 0.1;
                    cursorY += (mouseY - cursorY) * 0.1;
                    cursor.style.left = cursorX - 10 + 'px';
                    cursor.style.top = cursorY - 10 + 'px';
                    requestAnimationFrame(updateCursor);
                }
                updateCursor();
                
                // Cursor hover effects
                document.querySelectorAll('a, button, .btn-cyber').forEach(el => {
                    el.addEventListener('mouseenter', () => {
                        cursor.style.transform = 'scale(2)';
                        cursor.style.background = 'var(--cyber-secondary)';
                    });
                    el.addEventListener('mouseleave', () => {
                        cursor.style.transform = 'scale(1)';
                        cursor.style.background = 'var(--gradient-cyber)';
                    });
                });
            }
        }

        // Performance optimization
        function optimizePerformance() {
            // Lazy loading for images
            const images = document.querySelectorAll('img[data-src]');
            const imageObserver = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const img = entry.target;
                        img.src = img.dataset.src;
                        img.removeAttribute('data-src');
                        imageObserver.unobserve(img);
                    }
                });
            });
            
            images.forEach(img => imageObserver.observe(img));
            
            // Debounced resize handler
            let resizeTimeout;
            window.addEventListener('resize', () => {
                clearTimeout(resizeTimeout);
                resizeTimeout = setTimeout(() => {
                    // Recreate particles on resize
                    document.getElementById('particles').innerHTML = '';
                    createFloatingParticles();
                }, 250);
            });
        }

        // Easter eggs and interactions
        function initEasterEggs() {
            // Konami code
            let konamiCode = [];
            const konami = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65];
            
            document.addEventListener('keydown', (e) => {
                konamiCode.push(e.keyCode);
                if (konamiCode.length > 10) konamiCode.shift();
                
                if (konamiCode.join(',') === konami.join(',')) {
                    activateMatrixMode();
                }
            });
            
            // Double click on title for special effect
            document.querySelector('.cyber-title').addEventListener('dblclick', () => {
                triggerGlitchEffect();
            });
        }

        function activateMatrixMode() {
            document.body.style.animation = 'matrix-pulse 2s ease-in-out';
            setTimeout(() => {
                document.body.style.animation = '';
            }, 2000);
            
            // Add matrix style to body
            const style = document.createElement('style');
            style.textContent = `
                @keyframes matrix-pulse {
                    0% { filter: hue-rotate(0deg) saturate(1); }
                    50% { filter: hue-rotate(120deg) saturate(2); }
                    100% { filter: hue-rotate(0deg) saturate(1); }
                }
            `;
            document.head.appendChild(style);
        }

        function triggerGlitchEffect() {
            const elements = document.querySelectorAll('.cyber-title, .cyber-subtitle');
            elements.forEach(el => {
                el.style.animation = 'glitch 0.5s ease-in-out';
                setTimeout(() => {
                    el.style.animation = '';
                }, 500);
            });
        }

        // Initialize all animations and effects
        function initializeAnimations() {
            // Initialize AOS
            AOS.init({
                duration: 1000,
                once: true,
                offset: 100,
                easing: 'ease-out-cubic'
            });
            
            // Initialize all components
            initMatrixRain();
            initThreeJS();
            initSwiper();
            initScrollAnimations();
            initNavigation();
            initCursor();
            optimizePerformance();
            initEasterEggs();
            
            // Start particle animation
            setInterval(createFloatingParticles, 3000);
            createFloatingParticles();
        }

        // CSS Loading Animation
        const loadingStyle = document.createElement('style');
        loadingStyle.textContent = `
            @keyframes loading {
                0% { width: 0%; }
                50% { width: 70%; }
                100% { width: 100%; }
            }
            
            .scrolled {
                background: rgba(0, 0, 0, 0.95) !important;
                backdrop-filter: blur(20px);
                box-shadow: 0 10px 30px rgba(0, 255, 136, 0.3);
            }
            
            .hamburger {
                width: 30px;
                height: 24px;
                position: relative;
                cursor: pointer;
            }
            
            .hamburger span {
                display: block;
                position: absolute;
                height: 3px;
                width: 100%;
                background: var(--gradient-cyber);
                border-radius: 2px;
                opacity: 1;
                left: 0;
                transform: rotate(0deg);
                transition: .25s ease-in-out;
            }
            
            .hamburger span:nth-child(1) { top: 0px; }
            .hamburger span:nth-child(2) { top: 10px; }
            .hamburger span:nth-child(3) { top: 20px; }
        `;
        document.head.appendChild(loadingStyle);

        // Start initialization when DOM is ready
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', () => {
                setTimeout(initializeAnimations, 2000);
            });
        } else {
            setTimeout(initializeAnimations, 2000);
        }

        // Final touches - Add some ambient sounds (optional)
        function addAmbientEffects() {
            // Add subtle background music or ambient sounds
            // This can be implemented with Web Audio API
            
            // Add typing sound effects for interactive elements
            document.querySelectorAll('input, textarea').forEach(input => {
                input.addEventListener('keypress', () => {
                    // Add typing sound effect
                });
            });
        }

        // Call ambient effects
        setTimeout(addAmbientEffects, 3000);
        
        // Console Easter Egg
        console.log(`
        ╔══════════════════════════════════════════════════════════════╗
        ║                     SIJA-1 CYBER ACADEMY                    ║
        ║                  Elite Digital Academy 2025                 ║
        ║                                                              ║
        ║  🚀 Welcome to the Matrix, Developer!                      ║
        ║  💻 Built with: Web3 + AI + Blockchain Technologies        ║
        ║  ⚡ Created by: daffa-aditya-p                             ║
        ║  🌟 Value: $100,000,000 Premium Design                     ║
        ║                                                              ║
        ║  Try these Easter Eggs:                                      ║
        ║  • Konami Code: ↑↑↓↓←→←→BA                                  ║
        ║  • Double-click the title for glitch effect                 ║
        ║                                                              ║
        ╚══════════════════════════════════════════════════════════════╝
        `);
    </script>
</body>
</html>